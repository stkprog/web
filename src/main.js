import { createApp } from 'vue'
import './style.css'
import App from './App.vue'

// Entrypoint for the program
// Load the App.vue component into the div with the id "app"
createApp(App).mount('#app')
